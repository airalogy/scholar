# Scholar 更新日志

英文版本：[CHANGELOG.md](./CHANGELOG.md)

## [未发布]

## [5.0.0] - 2026-09-11

### 不兼容变更

- 论文、学者及相关接口返回的论文 DOI 现在可能为 `null`。调用方须处理无 DOI 论文，使用 Scholar UUID 或已核验的外部编号识别论文；系统不会生成占位 DOI。
- 启动 5.0 API 前须执行结构化书目迁移。Web/API 应作为同一版本整体部署；升级前备份数据库、上传文件和部署配置，仅回滚应用不能撤销数据库迁移。

### 变更

- 新增 v2 论文导入预览、明确确认、托管模式逐条审核及审计历史；双语管理界面支持标准 JSON 与显式配置的 CSV 映射。
- 机构专有字段具有数据类型、独立约束及公开、成员、管理员可见范围；论文详情展示结构化题名、作者贡献、机构署名、基金和有权查看的专有字段，不向访客暴露私有采集来源。
- 期刊与评价版本管理保留已发布历史；读者明确选择已发布版本查看期刊分区和论文指标，缺失值不视为否。
- 旧版提交、编辑和本地来源同步统一使用事务化书目服务；托管元数据审核保留原内容审核决定及附件，写入失败会回滚本次审核决定。
- CI 与发布验证强制执行 PostgreSQL 书目测试，本地推送前支持显式配置的隔离库检查；中英文导入示例使用实际 API Schema 验证。
- 侧栏底部新增无需登录的“关于”入口，显示前端构建版本与运行中的 API 版本，并提示版本不一致或服务器版本暂不可用。
- 明确身份核验的隐私边界：由申请人填写本人原编号，不展示其他账号的学号或工号；新增冲突标题与说明不重复的回归测试。
- 自部署论文元数据导入保留已有认领范围、附件、作者人员绑定和审核决定，仅对新建的机构认领自动批准。
- 新增事务化书目写入服务，以及多语言题名、外部编号、作者、署名机构、基金、期刊版本和机构自定义字段的结构化存储；迁移保留旧题名、DOI 和作者关系，并增加隔离 PostgreSQL 测试。
- 导入后的检索索引更新改为持久化任务，支持租约、重试上限和版本检查，避免进程中断丢失任务或旧任务错误完成新版本更新。

## [4.1.0] - 2026-09-11

### 新增

- 机构 owner 和平台管理员可为同一人员核验附加学号或工号，支持撤销、恢复及审计记录，保留原主编号、账号、档案和论文绑定。
- 学校 SSO 认证成功但账号绑定冲突时，可提交私人的身份核验申请，支持原编号声明、状态查询、补充说明、管理员通过或驳回及中英文界面。

### 安全

- 不根据姓名或邮箱自动合并账号。短期核验凭据只保存哈希，不能访问用户账号；身份操作拒绝系统凭证、跨机构访问、越权操作及自我核验。
- 撤销编号会使绑定该编号版本的新式 SSO 会话失效；升级前及密码登录会话保留原有效期。已保留编号不能转给另一人，导入也不能替换已关联账号的主编号来绕过核验。
- 将发布安全检查发现的开发及构建依赖 Vitest、SVGO 和 js-yaml 更新至已修复版本。

### 修复

- PostgreSQL 事务锁查询改为返回驱动兼容的类型，避免 Prisma 适配器无法反序列化 `void` 导致写入失败。
- 人员更新、成员及预开通修改、组织导入和部署工具统一先获取身份锁再写人员行，避免与并发 SSO 登录发生死锁。

### 数据库与部署

- 新增主编号及附加编号、短期核验凭据、申请和审计事件。增量迁移保留原人员及论文绑定，不推断真实人员关联；启动新 API 前须执行迁移。

### 质量保障

- 新增 PostgreSQL 集成和双语界面测试，覆盖审核、权限、重放、并发操作、撤销、导入、凭据过期、草稿保留及请求取消；CI 和发布验证均执行身份流程与无损升级检查。

## [4.0.1] - 2026-09-11

### 变更

- 将反馈入口从聊天样式的悬浮按钮移至侧栏底部的使用文档旁，图标和文字自动整体居中，采用书写图标，并通过支持键盘操作的弹窗填写意见反馈。

### 修复

- 补丁版本校验允许省略不适用的更新日志分类，同时继续拒绝中英文章节或条目数量不一致的情况。

### 质量保障

- 新增反馈回归测试，覆盖键盘焦点、草稿保留、输入校验、防重复提交、移动端导航和语言切换。

### 数据库与部署

- 无数据库迁移、鉴权变更或新增部署配置。API、Web 与随包文档整体升级至 4.0.1；数据库结构与 4.0.0 保持兼容。

## [4.0.0] - 2026-09-10

### 新增

- 机构人员现使用一个不区分大小写的规范内部 ID；人员可在用户账号之前存在，分别关联 user 和 scholar，并在首次可验证登录后自动显示预绑定论文。

### 变更

- 恢复论文全文混合检索：已审核 PDF 正文、确定性 BM25 评分和向量检索共用索引，并向论文阅读助手提供相关证据。
- Scholar 现在明确以单机构为产品边界：每个机构自托管实例或 Airalogy Managed 租户只服务一个已配置机构，运行时路由不再提供机构切换。
- 机构 SSO 现按规范内部 ID 字段解析身份，在事务中创建或绑定账号，默认只授予普通成员角色，并拒绝只依赖邮箱或存在冲突的身份认领。
- Airalogy Managed 租户和机构自托管实例共用同一代码库，导入数据分别执行托管审核和直接生效策略。
- 论文、学者、实验室和已发布学位论文页面现可免登录浏览；只有在用户执行账号相关操作时才请求登录。
- 仓库工程与运维文档现完整提供 `docs/en` 和 `docs/zh` 两套中英文目录，并自动检查语言配对、本地链接和产品文档边界。
- 版本化产品文档的可见标题与首页现统一使用完整的 Airalogy Scholar 名称。

### 安全

- 将 Fastify 及受影响的 `urllib`、`mysql2`、`fast-uri`、`qs` 依赖链更新至已修复版本；发布构建现在也执行与 PR 和推送前检查相同的生产依赖安全审计。
- 只有显式开启 `ALLOW_APPROVED_PDF_MODEL_PROCESSING=true`，才允许将已审核 PDF 片段发送给所配置的模型或 embedding 服务；未审核和其他机构文件仍不可处理。
- 内容、审核、文件、导入、时间线和集成凭证路由现全部约束到已配置机构，包括平台管理员队列和历史数据。
- 公开学者和论文 API 不暴露机构内部 ID；系统集成 JWT 仍不允许访问用户和时间线接口。
- 匿名 API 访问仅返回已审核或已发布的公开记录，不签发受保护文件链接，不暴露私有作者信息，并继续拒绝系统集成凭证访问用户端接口。
- 生产依赖安全审计现会在每次推送前运行，并将 Prisma 配置链路中的受影响间接依赖覆盖为已修复的 `deepmerge-ts` 8 版本线。

### 数据库与部署

- 发布源码导出现会逐字节保留所有已发布迁移，并将增量迁移纳入校验和清单，确保全新安装与已有数据库使用相同迁移历史。
- 新增 BM25 索引元数据并保留原有向量；升级后可通过共用论文索引命令重新索引已有论文。
- 新增从旧组织人员和学者映射到 `institution_people` 的无损迁移，新增结构化身份绑定审计事件，并将论文作者绑定改为直接指向机构人员。
- 用 `MANAGEMENT_MODE`、`INSTITUTION_SLUG`、`CONTENT_ACCESS_MODE` 和 `INSTITUTION_SSO_INTERNAL_ID_FIELD` 替换旧部署变量，并在部署文档与产品文档中统一说明单机构模型。
- 机构部署可明确配置 Scholar Docker IPv4 子网和可选网关；预检会拒绝与声明的保留网段、宿主机路由及现有 Docker 网络重叠的配置。

### 质量保障

- 版本接口与部署元数据测试现统一读取产品版本，并增加拒绝不匹配发布标签的回归覆盖。
- 新增 PostgreSQL 事务化升级样例，核对已有身份、同名成员、待领取邀请、任职、论文作者绑定和检索索引的保留情况，并在 CI 和发布验证中执行。
- 容器 CI 改为从 `VERSION` 读取产品版本，避免写死版本号。

### 不兼容变更

- 已有部署启动单机构版本前必须迁移配置，详见[升级说明](docs/zh/deployment.md#从-300-升级到单机构版本)；旧多机构实例需要先制定明确的数据拆分方案。
- 移除机构加入申请接口；公开配置响应以 `tenancyMode`、`managementMode`、`contentAccess` 和 `institution` 替换 `deploymentMode`。
- 身份迁移会调整作者绑定和机构人员表。旧 API 镜像不能直接运行于迁移后数据库；回滚必须恢复升级前数据库备份及对应应用配置。

## [3.0.0] - 2026-08-14

### 新增

- 新增机构中立的学术信息平台，覆盖学者、实验室、论文、学位论文、研究年谱、收藏、讨论和检索。
- 新增可配置的机构品牌、本地账号控制、通用机构 SSO、成员角色、细粒度权限和实验室管理。
- 新增中英文分级学科目录、稳定编码、机构本地映射、数据库动态筛选和结构化学者资料。
- 新增论文与学位论文共用的版本绑定、多级内容审核，支持退回修改、重新提交、发布控制和不可变审核记录。
- 新增机构范围批量导入 API、短期集成凭证、幂等保护、浏览器端 CSV 导入和导入历史界面。
- 新增可选的 OpenAI-compatible 对话、学者推荐、PostgreSQL/pgvector 检索、受保护文档阅读和管理员控制的研究年谱生成。
- 新增版本一致的中英文产品文档、OpenAPI 文档和虚构演示数据。
- 新增由服务器授权的管理控制台，分层展示平台、机构、审核、导入和实验室能力，并提供明确的无权访问页面。

### 安全

- 在数据读取和写入操作中落实机构、实验室、提交者、审核者和平台管理员边界。
- 新增受保护文件策略、文件签名检查、配额、短期访问链接、敏感日志脱敏、请求限流以及安全的 Markdown 与 URL 渲染。
- 新增密钥形态扫描、依赖漏洞审计、第三方许可证审查和机构专属标记公开检查。
- 将集成凭证限制在明确的权限范围和机构内，并支持轮换、吊销、过期控制及明文密钥仅显示一次。
- 新增固定到提交的 GitHub Actions、按任务授予的发布权限、软件物料清单，以及已发布容器镜像的构建来源证明。

### 数据库与部署

- 建立干净的 PostgreSQL 17 与 pgvector 数据库基线，并以一个 Scholar 3.0.0 初始 Prisma 迁移表示。
- 新增由部署方持有的数据库、上传文件和备份持久化，支持内置或外部管理的 PostgreSQL。
- 新增版本化、固定镜像摘要的 API 与 Web 镜像、发布清单、迁移任务、健康检查、备份感知升级和离线镜像交付。
- 新增可配置本地存储、机构管理的对象存储、反向代理集成，以及适用于私有或断网环境的镜像仓库配置。

### 质量保障

- 新增格式、代码、类型、单元、集成、迁移、部署、生产构建和构建产物冒烟检查。
- 新增发布身份校验，将产品版本、Git 提交、数据库迁移、产品文档和组件镜像精确摘要绑定在一起。
- 新增数据库完整性审计，以及鉴权、审核流程、导入、检索索引、文件隔离和 AI 生成内容安全的回归覆盖。
- 新增发布源码的从零验证，覆盖依赖安装、客户端生成、测试、构建和空 schema 迁移。
